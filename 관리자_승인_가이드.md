# 인명부(人名簿) 관리자 승인 가이드

> **🆕 이제 사이트 안에서 직접 수정할 수 있어요!**
> 아래 "실시간 관리 패널" 부분을 먼저 읽어보세요. json 손편집은 예비 수단으로 남겨둡니다.

---

# 실시간 관리 패널 (추천)

사이트 **맨 아래 푸터의 「管」 글자**를 누르면 관리 패널이 열립니다.
여기서 인명부·현상수배를 **양식 채우듯 추가/수정/삭제**할 수 있고, 저장하면 약 1분 내에 모두에게 반영됩니다.

## 처음 한 번만: 관리 열쇠(토큰) 만들기

사이트 자체에는 비밀번호를 둘 수 없어서(코드가 공개됨), 깃허브 토큰이 비밀번호 역할을 합니다.
**토큰은 내 브라우저에만 저장되고 사이트에 공개되지 않습니다.**

1. 깃허브 로그인 → 오른쪽 위 프로필 사진 → **Settings**
2. 왼쪽 맨 아래 **Developer settings** → **Personal access tokens** → **Fine-grained tokens** → **Generate new token**
3. 설정:
   - **Token name**: `gangho-admin` (아무거나)
   - **Expiration**: 90 days 또는 원하는 기간 (만료되면 다시 만들면 됩니다)
   - **Repository access**: **Only select repositories** → 사이트 저장소만 선택 ⭐중요
   - **Permissions** → Repository permissions → **Contents** → **Read and write** ⭐중요
4. **Generate token** → 나오는 `github_pat_...` 문자열을 복사 (이 화면을 벗어나면 다시 못 봐요)

## 사용법

1. 사이트 푸터의 **「管」** 클릭
2. 저장소: `내아이디/저장소명` (깃허브 주소로 열면 자동으로 채워져요)
3. 관리 열쇠: 복사해둔 토큰 붙여넣기 → **[열기]**
4. 인명부/현상수배 탭에서 **[＋ 새로 추가]**, **[수정]**, **[삭제]**
5. **[저장 (사이트 반영)]** 누르면 끝 — 약 1분 내 반영

- "이 브라우저에 열쇠 기억하기"를 켜면 다음부터 바로 열려요. **내 폰/컴퓨터에서만 켜세요.**
- 공용 컴퓨터에서 썼다면 **[잠그기]**를 눌러 열쇠를 지우세요.
- ⚠️ 토큰은 절대 다른 사람에게 보내거나 채팅방에 올리지 마세요. 유출됐다 싶으면 깃허브에서 그 토큰을 삭제(Revoke)하면 즉시 무효화됩니다.

---

# (예비) json 손편집 방식

캐릭터 승인·수정·삭제는 전부 깃허브의 **characters.json** 파일 하나로 관리합니다.
이 파일에 캐릭터를 추가하면 = 승인, 지우면 = 삭제, 고치면 = 수정입니다.
저장(Commit) 후 1분 안에 사이트 인명부에 자동 반영됩니다.

---

## 처음 한 번만: characters.json 올리기

1. 깃허브 저장소 열기
2. **Add file ▸ Upload files** → `characters.json` 파일을 드래그
3. **Commit changes**

(index.html과 같은 위치에 있어야 합니다.)

---

## 캐릭터 승인하는 법

1. 저장소에서 **characters.json** 클릭 → 오른쪽 위 **연필(✏️)** 아이콘 클릭
2. 대괄호 `[ ]` 안에, 아래 틀을 복사해서 붙여넣고 제출받은 내용으로 채우기:

```json
  {
    "이름": "여기에 이름",
    "별호": "",
    "나이": "",
    "성별": "",
    "신장/체중": "",
    "소속": "",
    "무공": "",
    "경지": "",
    "외형": "",
    "성격": "",
    "특징": "",
    "과거사": "",
    "무기": "",
    "선호/불호": "",
    "한마디": "",
    "진영": "정"
  }
```

3. **Commit changes** 클릭 → 끝! 1분 뒤 인명부에 나타납니다.

### 꼭 지켜야 하는 규칙 (JSON 문법)

- 캐릭터와 캐릭터 사이에는 **쉼표(,)** 를 넣습니다:
  ```json
  [
    { "이름": "첫 번째" },
    { "이름": "두 번째" }
  ]
  ```
- **맨 마지막 캐릭터 뒤에는 쉼표를 넣지 않습니다.**
- 따옴표(`"`)는 지우면 안 됩니다.
- 비워둘 항목은 `""` 그대로 두거나 그 줄을 통째로 지워도 됩니다.
- `"진영"`은 카드 왼쪽 색깔을 정합니다: `"정"`(파랑) / `"사"`(보라) / `"마"`(붉음) / `"낭인"`(회색).
  안 쓰면 소속 이름을 보고 자동으로 정해집니다.

---

## 수정 / 삭제

- **수정**: characters.json 연필 아이콘 → 해당 캐릭터의 글자를 고치고 Commit
- **삭제**: 그 캐릭터의 `{ ... }` 블록을 (앞뒤 쉼표 포함해서 딱 맞게) 지우고 Commit

---

## 문제가 생겼을 때

- 인명부에 "불러올 수 없습니다"가 뜨면 → 대부분 **쉼표/따옴표 실수**입니다.
  characters.json을 열어 마지막에 고친 부분을 확인하세요.
- JSON이 맞는지 미리 검사하고 싶으면: 내용을 복사해서 https://jsonlint.com 에 붙여넣고 Validate.
- 예시 캐릭터(楚流香)는 형식 참고용이니, 실제 운영 시작할 때 지우면 됩니다.

---

## 전체 흐름 요약

```
참가자: 사이트 생성기 작성 → [복사] → 오픈채팅으로 관리진에게 제출
관리진: 검토 → characters.json에 붙여넣고 Commit → 인명부에 자동 게재
```

---

# 현상수배(懸賞手配) 게시판 관리

수배범 관리도 인명부와 똑같은 방식입니다. **wanted.json** 파일 하나로 관리합니다.
(처음 한 번 wanted.json을 저장소에 올려두세요. index.html과 같은 위치.)

## 수배범 등록 틀

wanted.json의 대괄호 `[ ]` 안에 붙여넣고 채우세요:

```json
  {
    "별호": "",
    "이름": "",
    "죄목": "",
    "현상금": "",
    "위험 등급": "",
    "경지": "",
    "소속": "",
    "인상착의": "",
    "행적": "",
    "비고": ""
  }
```

- 카드 오른쪽에 **현상금**(금색)과 **위험 등급**(붉은색)이 크게 표시됩니다.
- 위험 등급을 비우면 경지가 대신 표시됩니다.
- 쉼표/따옴표 규칙은 인명부와 동일합니다. (마지막 항목 뒤 쉼표 금지)
- 잡힌 수배범은 블록을 지우면 게시판에서 내려갑니다.
- 예시 수배범(곽철산)은 실제 운영 시작할 때 지우면 됩니다.
